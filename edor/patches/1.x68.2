diff '--exclude=.git' -ura --new-file edor/s/bar.c edornew/s/bar.c
--- edor/s/bar.c	2025-04-10 09:07:41.906591376 +0000
+++ edornew/s/bar.c	2025-04-10 09:26:23.338936211 +0000
@@ -115,9 +115,9 @@
 		size_t m=i;
 		do{
 			i=m;
-			char*errors=split_write(&m,f,_off,&majorerror);
+			const char*errors=split_write(&m,f,_off,&majorerror);
 			if(errors!=nullptr){
-				print_error(errors);
+				print_error((char*)errors);
 				if(majorerror/*true*/)return false;
 				*no_errors=false;
 			}
diff '--exclude=.git' -ura --new-file edor/s/base.h edornew/s/base.h
--- edor/s/base.h	2025-04-10 09:07:41.916591399 +0000
+++ edornew/s/base.h	2025-04-10 09:26:23.348936232 +0000
@@ -100,7 +100,7 @@
 bool split_readprefs(int);
 void split_freeprefs();
 bool split_write_init(char*);
-char* split_write(size_t*,int,unsigned int*,bool*);
+const char* split_write(size_t*,int,unsigned int*,bool*);
 void split_write_free();
 //bar
 bool swrite(int,void*,unsigned int);
diff '--exclude=.git' -ura --new-file edor/termux edornew/termux
--- edor/termux	2025-04-10 09:07:42.716593259 +0000
+++ edornew/termux	2025-04-10 09:26:24.188938016 +0000
@@ -1,4 +1,4 @@
-
+git pull origin master && \
 autoreconf -i && \
 if [ ! -e configure ]; then
 	pfx="--prefix=$PREFIX" ./cf
